<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech-Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header id="main-header" class="display-flex justify-space-between align-center p-2">
      <h1>
        <a class="main-title" href="/">Talking Tech</a>
      </h1>
      <nav class="display-flex justify-space-between align-center">
        <a class="no-button" href="/">Home</a>
        <a class="no-button" href="/dashboard">Dashboard</a>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a class="no-button" href="/login">login</a>
        {{/if}}
      </nav>
    </header>

    <main id="main-container" class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer id="main-footer">
      <p class="discreet">Created by: NateIrvision Productions - 2021</p>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
